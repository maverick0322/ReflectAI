name: SonarCloud Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: SonarQube Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Instalar dependencias
        working-directory: ./reflectai
        # Fix de Copilot: usar npm ci para mayor estabilidad
        run: npm ci

      - name: Correr pruebas y generar Cobertura
        working-directory: ./reflectai
        # ¡Este comando AHORA SÍ funcionará porque Arturo ya configuró Vitest!
        run: npm run test -- --coverage

      - name: SonarCloud Scan
        # Fix de Copilot: usar la versión v2 en lugar de @master
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}